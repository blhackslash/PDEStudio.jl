name: CI

on:
  push:
    branches: [ "main" ]
    tags: ["*"]
  pull_request:
    branches: [ "main" ]

concurrency:
  # Cancel intermediate runs on the same branch/PR
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

jobs:
  test:
    name: Julia ${{ matrix.version }} - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        version:
          - '1.10'
          - '1.12'
          - '1' # Tests latest stable (e.g. 1.13)
        os:
          - ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Julia
        uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}
          
      - uses: julia-actions/cache@v2

      # 1. Install virtual display server for GLMakie headless testing
      - name: Install Xvfb
        run: sudo apt-get update && sudo apt-get install -y xvfb
      - name: Install unregistered dependencies
        run: |
          julia --color=yes -e '
            using Pkg
            Pkg.add(url="https://github.com/blhackslash/PDEStudioCore.jl.git")
            '
      - uses: julia-actions/julia-buildpkg@v1
      
      # 2. Wrap the runtest action in xvfb-run
      - uses: julia-actions/julia-runtest@v1
        with:
          prefix: xvfb-run

      # 3. Process the coverage data generated by the tests
      - uses: julia-actions/julia-processcoverage@v1

      # 4. Upload the coverage report to Codecov
      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: blhackslash/PDEStudio.jl

  docs:
    name: Documentation
    runs-on: ubuntu-latest
    needs: test
    permissions:
      actions: write
      contents: write
      statuses: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Julia
        uses: julia-actions/setup-julia@v2
        with:
          version: '1'

      - uses: julia-actions/cache@v2

      # 5. Provide a virtual display to Documenter for plotting during doc build
      - name: Install Xvfb
        run: sudo apt-get update && sudo apt-get install -y xvfb

      - name: Build and deploy documentation
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          julia --project=docs -e '
            using Pkg
            Pkg.develop(PackageSpec(path=pwd()))
            Pkg.instantiate()'
          xvfb-run julia --project=docs docs/make.jl
